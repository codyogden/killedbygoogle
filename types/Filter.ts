import { ProductType } from './Product';

// export type FilterType = ProductType | 'all';

export enum FilterType {
    ALL = 'all',
    APP = 'app',
    SERVICE = 'service',
    HARDWARE = 'hardware',
}
